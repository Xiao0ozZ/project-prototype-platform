# Project Core

项目包、页面定义、文档和 PRD 关联的纯 Node 核心。该目录不依赖 Vue、Vite 或浏览器 API，供以下入口复用：

- Vite 开发与构建插件；
- 项目包命令行工具；
- 自动化测试与后续 MCP 接口。

`html-prototype-shell` 是可整目录复制的独立工具，不导入本包。它只与核心保持配置概念和测试契约一致，不能形成运行时依赖。

`schemas/` 是对外的机器可读契约，`src/` 保留包含跨字段、文件存在性和路径边界检查的运行时校验。JSON Schema 与运行时校验必须同步维护。

核心模块只负责领域规则和文件系统能力，不包含 HTTP 路由、Vite 热更新、Vue 组件或界面状态。
